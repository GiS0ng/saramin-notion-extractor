# SEO/AEO/GEO 콘텐츠 파이프라인 설계 문서

> saramin-notion-extractor를 "수집기"로 유지한 채, 그 뒤에 붙일 분석·발행·모니터링 파이프라인 설계안.
> 이 문서는 범용(도메인 무관) 아키텍처를 확정하는 것이 목적이며, 특정 도메인(예: 리마인더의 법무/특허/세무 고객사 데이터) 적용은 별도 단계에서 진행한다.

---

## 1. 배경

- 기존 저장소 `saramin-notion-extractor`는 사람인 채용공고를 텍스트/이미지(OCR)로 읽어 공통 JSON으로 정규화하고, 공고링크(`rec_idx`) 기준으로 Notion에 upsert하는 개인용 Chrome 확장 프로그램이다.
- 이 구조(수집 → 정규화 → 저장)는 SEO/AEO/GEO 콘텐츠 파이프라인의 앞단으로 그대로 재사용 가능하다는 판단 하에, 뒷단(분석 → 발행 → 모니터링)을 추가 설계한다.
- **핵심 전제**: Notion에 구조화된 데이터를 저장하는 것 자체는 SEO/AEO/GEO가 아니다. 이 데이터를 가공해 실제 검색 가능한 공간(웹/블로그/CMS)에 발행하고, 발행 후 성과를 측정해 되먹이는 루프까지 있어야 완성된 시스템이다.

---

## 2. 현재 시스템 (Baseline — 변경하지 않음)

```
사람인 (검색 URL 여러 개)
   │
   ▼
Chrome Extension (saramin-notion-extractor)
   - 일반/이어보기/이미지형 공고 → 동일 JSON 구조로 변환
   - 이미지형은 Tesseract.js OCR (한국어/영어)
   - 검색 URL {id, name, url, enabled} 다중 관리
   - 최근 N일 / URL당 최대 페이지 / 전체 최대 공고 수 제한
   - 429·5xx·네트워크 오류만 지수 백오프+jitter 재시도
   - 중단 후 재개 가능한 커서 {searchIndex, currentPage, nextJobIndex}
   - 주 1회 예약(현지시간), Chrome 재시작 시 보충 실행 1회
   - 실행 이력 20건 + 실행당 에러 20건 보관
   │
   ▼
Notion RAW DB (rec_idx 기준 upsert, 중복 방지)
```

**역할 고정**: 이 확장 프로그램은 앞으로도 "수집기" 역할만 담당한다. LLM API 키나 분석 로직을 여기에 절대 넣지 않는다 (이유는 4.1 참조).

---

## 3. 목표 아키텍처

```
                         사람인 (또는 향후 다른 소스)
                                │
                    ┌───────────────────────┐
                    │ Chrome Extension      │  ← 역할 고정: "수집기"만
                    │ (기존 saramin-notion-  │
                    │  extractor, 무변경)    │
                    └───────────┬───────────┘
                                │ 공통 JSON
                                ▼
                    ┌───────────────────────┐
                    │ Notion RAW DB         │  ← 기존 그대로 유지
                    └───────────┬───────────┘
                                │
                    ═══════════════════════════════
                     SEO / AEO / GEO 분석 파이프라인
                    ═══════════════════════════════
                                │
                                ▼
                    ┌───────────────────────┐
                    │ Backend (FastAPI)     │  ← 수집기와 분리 (보안)
                    │  - 정제 / 중복 제거    │
                    │  - 키워드 / Entity 추출│
                    │  - 검색의도 분석       │
                    └───────────┬───────────┘
                                ▼
                    ┌───────────────────────┐
                    │ LLM 분석 (1콜,         │  ← SEO/AEO/GEO 3번 분리 호출 X
                    │ 구조화 출력 스키마로   │     비용·속도 위해 1회 호출로
                    │ 3영역 동시 태깅)       │     스키마 강제 출력
                    └───────────┬───────────┘
                                ▼
              ┌─────────────────────────────────────┐
              │ PostgreSQL (개별 문서 + 정형 데이터)   │  ← 집계 쿼리용
              └─────────────────┬───────────────────┘
                                ▼
              ┌─────────────────────────────────────┐
              │ 집계 레이어 (배치, 주기 실행)          │  ← 핵심 콘텐츠 전략
              │ N건 모아 통계/트렌드 콘텐츠 자동 생성   │     (데이터저널리즘형,
              │ (예: 키워드/기술 등장 빈도, 추세 변화) │      SEO/GEO에 실제로 강함)
              └─────────────────┬───────────────────┘
                                ▼
                    ┌───────────────────────┐
                    │ Notion 분석 DB        │  ← 사람이 보는 화면
                    │ (에디토리얼 워크플로우)│     (섹션 5 참조)
                    └───────────┬───────────┘
                                ▼
                    ┌───────────────────────┐
                    │ 검수 (Human-in-loop)  │  ← "AI 산출물을 판단 없이
                    └───────────┬───────────┘     그대로 내지 않는다" 원칙
                                ▼
                    ┌───────────────────────┐
                    │ Web / Blog / CMS 발행 │
                    └───────────┬───────────┘
                                ▼
                    ┌───────────────────────┐
                    │ GEO 모니터링 (역방향)  │  ← 루프를 닫는 단계
                    │ 발행 후 ChatGPT /      │
                    │ Perplexity / AI        │
                    │ Overview 재질의해      │
                    │ 인용 여부 추적         │
                    └───────────┬───────────┘
                                │
                                └──────────▶ Notion 분석 DB "성과" 컬럼에
                                             피드백 → 다음 가설에 반영
```

---

## 4. 설계 원칙

### 4.1 수집기와 분석기를 분리한다
Chrome 확장 프로그램은 사용자 브라우저에서 실행되므로 코드와 로컬 저장값이 리버스엔지니어링에 노출되기 쉽다. LLM API 키를 확장 프로그램에 넣으면 키 유출 사고로 이어질 수 있다. 따라서:
- 확장 프로그램: 수집 + Notion RAW 저장까지만.
- 별도 백엔드(FastAPI): LLM 호출, 분석, 발행 로직을 전담.

### 4.2 두 종류의 분석을 분리한다
- **개별 문서 단위 분석**: 공고(또는 원본 콘텐츠) 하나에 SEO/AEO/GEO 태그를 붙이는 것. 원자료를 구조화된 메타데이터로 바꾸는 단계.
- **집계 분석**: 여러 건을 모아 통계/트렌드로 만드는 것. 원본 데이터 기반 통계 콘텐츠는 백링크·AI 인용을 가장 잘 받는 콘텐츠 유형 중 하나이므로, 이 레이어가 실제 발행 콘텐츠의 핵심이 된다.

이 둘은 서로 다른 배치 주기와 저장소를 가진다 (개별 분석은 문서 유입 시 즉시, 집계는 주기적 배치).

### 4.3 발행으로 끝내지 않고 루프를 닫는다
콘텐츠를 만들어 발행하는 것으로 끝나면 "실험"이 되지 않는다. 발행 후 실제로 AI 검색(ChatGPT/Perplexity/구글 AI Overview 등)에서 인용되는지 주기적으로 재질의하여 추적하고, 그 결과를 다음 콘텐츠 가설에 반영한다. 이 되먹임 루프가 있어야 "가설 → 실험 → 숫자로 결론"이라는 구조가 완성된다.

### 4.4 법적/운영 리스크는 정책 계층으로 명시한다
다이어그램의 모든 화살표(특히 외부 사이트 접근)에 다음 규칙을 적용한다.
- 새 소스를 붙이기 전 해당 사이트의 이용약관을 확인한다.
- 요청 간격 제한과 재시도 백오프(기존 로직 재사용)를 유지해 대상 서버에 부하를 주지 않는다.
- 원문을 대량으로 복제·재게시하지 않는다 — 정제된 사실·통계·구조화 데이터만 발행한다.
- 가능하면 공식 API가 있는 소스를 우선한다.
- 여러 플랫폼(예: 채용 사이트 다중 연동처럼 규모가 커지는 경우)을 조직적으로 크롤링하는 방향으로 확장할 때는 특히 신중히 검토한다 (참고: 잡코리아 vs 사람인 크롤링 소송, 대법원 2017.8 확정 — 무단 크롤링 후 재게시가 부정경쟁행위로 인정된 사례).

---

## 5. 데이터 스키마

### 5.1 개별 문서 분석 출력 (LLM 구조화 출력)

```json
{
  "source_id": "원본 레코드 식별자 (예: rec_idx)",
  "source_url": "원본 URL",
  "keywords": ["핵심 키워드 목록"],
  "search_intent": "검색 의도 요약",
  "seo": {
    "title": "SEO 제목",
    "description": "메타 설명",
    "headings": ["제안 H2/H3 구조"]
  },
  "aeo": {
    "questions_and_answers": [
      { "q": "예상 질문", "a": "간결한 답변" }
    ]
  },
  "geo": {
    "entities": ["핵심 개체명"],
    "claim_evidence_pairs": [
      { "claim": "주장", "evidence": "근거 문장", "source": "출처" }
    ],
    "summary": "AI가 인용하기 쉬운 요약문"
  }
}
```

### 5.2 집계 콘텐츠 출력 (배치 결과)

```json
{
  "batch_id": "집계 배치 식별자",
  "sample_size": 100,
  "period": "집계 대상 기간",
  "stats": [
    { "keyword": "예시 키워드", "frequency_pct": 0.0 }
  ],
  "generated_content_draft": {
    "seo_title": "...",
    "aeo_qna": [ { "q": "...", "a": "..." } ],
    "geo_summary": "..."
  }
}
```

### 5.3 Notion 분석 DB 컬럼

| 필드 | 용도 |
|---|---|
| 원본 제목/식별자 | 원본 참조 |
| 원문 URL | 출처 |
| 핵심 키워드 | SEO |
| 검색 의도 | SEO |
| SEO 제목 / Meta Description | SEO |
| 사용자 예상 질문 / 답변 | AEO |
| 핵심 Entity | GEO |
| 근거 문장 / 출처 | GEO |
| AI 요약 | AEO/GEO |
| Content Score | 내부 평가 지표 (표준 점수 아님, 자체 정의) |
| 검수 상태 | 워크플로우 |
| 발행 상태 | CMS 연동 |
| 발행 후 인용 여부 / 순위 | GEO 모니터링 피드백 (4.3) |

---

## 6. 다음 단계 (TODO)

- [ ] 백엔드(FastAPI) 최소 스캐폴드 구성 — `/jobs` 수신 엔드포인트, PostgreSQL 연결
- [ ] LLM 구조화 출력 프롬프트 및 스키마 검증 로직 작성 (5.1 스키마 기준)
- [ ] 집계 배치 잡 설계 (실행 주기, 최소 표본 크기 정의)
- [ ] Notion 분석 DB 템플릿 생성 (5.3 컬럼 기준)
- [ ] GEO 모니터링 재질의 스크립트 설계 (재시도/백오프 로직은 기존 수집기 로직 재사용 검토)
- [ ] **도메인 적용** (별도 단계): 집계 레이어의 입력 소스와 발행 CMS의 대상 독자를 실제 서비스 도메인에 맞게 치환

## 7. 미해결 질문

- LLM 분석을 실시간(문서 유입 시)으로 할지, 배치로 모아서 할지 — 비용/속도 트레이드오프 결정 필요
- 집계 배치의 "충분한 표본 크기" 기준 (예: 최소 N건 이상일 때만 통계 콘텐츠 생성)
- GEO 모니터링 재질의 대상 플랫폼별 공식 API 유무 확인 필요 (약관 위반 리스크 최소화)
- Content Score 산정 기준 (내부 지표 설계)
