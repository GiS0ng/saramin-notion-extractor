# CLAUDE.md

## 테스트 작성 워크플로우

이 프로젝트에서 새 기능을 구현할 때, 테스트 코드는 Claude Code가 직접 작성하지 않고 `codex` CLI로 별도 작성/검증한다.

- 구현: Claude Code가 담당
- 테스트: `codex exec "<프롬프트>"` (비대화형 모드)로 Codex가 독립적으로 작성·검증

같은 모델이 구현과 테스트를 둘 다 작성하면 그 모델의 맹점(같은 가정, 같은 실수)을 테스트가 잡아내지 못한다. 다른 모델이 테스트를 작성해야 실질적인 2차 검증이 된다.

실행 환경: `codex` CLI는 `/home/gi/.local/bin/codex`에 설치되어 있다 (codex-cli 0.147.0 기준).
